import 'package:flutter/material.dart';
import 'community_feed_page.dart';

/// CommunityFeedTab is a wrapper tab for displaying the community feed in a tabbed interface.
class CommunityFeedTab extends StatelessWidget {
  /// Creates a CommunityFeedTab.
  const CommunityFeedTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const CommunityFeedPage();
  }
}
