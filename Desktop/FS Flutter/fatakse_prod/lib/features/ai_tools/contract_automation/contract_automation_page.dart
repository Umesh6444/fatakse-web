import 'package:flutter/material.dart';

class ContractAutomationPage extends StatelessWidget {
  const ContractAutomationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Smart Contract & Payment Automation')),
      body: const Center(
        child: Text('AI contract generation and review coming soon!'),
      ),
    );
  }
}
