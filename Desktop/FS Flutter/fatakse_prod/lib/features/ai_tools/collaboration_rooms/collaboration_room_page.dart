import 'package:flutter/material.dart';

class CollaborationRoomPage extends StatelessWidget {
  const CollaborationRoomPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('AI Collaboration Room')),
      body: const Center(
        child: Text('Real-time brainstorming and planning coming soon!'),
      ),
    );
  }
}
