import 'package:flutter/material.dart';

class EventBlueprintPage extends StatelessWidget {
  const EventBlueprintPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dynamic Event Blueprint Generator')),
      body: const Center(child: Text('AI event planning coming soon!')),
    );
  }
}
